# 원본(backend-main) 대비 수정한 버그

원본 `backend-main.zip` 을 검토·재편하면서 고친 것들입니다.
각 항목은 원본 코드에서 실제로 확인한 내용입니다.

---

## A. 파싱 결과가 틀리던 것

### A-1. HWP/HWPX 표가 3행에서 잘림

`rag/tables/markdown.py`

```python
r"(\|[^\n]+\|\n\|[-:\s|]+\|(?:\n\|[^\n]+\|)*)"
#                  ^^^ \s 가 개행까지 삼킨다
```

구분선 문자 클래스에 `\s` 가 들어 있어 개행을 소비합니다. 그 결과 구분선
매칭이 다음 행까지 먹어 **표가 3행에서 끊겼습니다.**

```python
r"(\|[^\n]+\|\n\|[-:| \t]+\|(?:\n\|[^\n]+\|)*)"
```

검증: 4행짜리 표(`구분/예산/인원`)가 온전히 나오는 것을 `/convert/markdown`
응답으로 확인.

### A-2. HWPML 표 셀 텍스트가 본문에 중복

`converters/hwp/hwpml.py:77`

```python
for text_elem in p_elem.findall(".//TEXT"):
#                                ^^^ CELL 내부까지 재귀 수집
```

`.//` 는 하위 전체를 훑으므로 표 안 `CELL` 의 `TEXT` 까지 본문 문단으로
수집됩니다. 표는 별도로 렌더되므로 **같은 내용이 두 번** 나왔습니다.

원본에도 `text_elem.find("TABLE")` 검사는 있었지만, 이건 "TEXT 가 TABLE 을
직접 자식으로 갖는 경우"만 걸러냅니다. 표 **안쪽** 의 TEXT 는 그 조건에
걸리지 않아 그대로 통과했습니다.

TABLE 서브트리에 속한 노드를 미리 모아 제외하도록 고쳤습니다.

```python
inside_table = {
    id(node)
    for tbl in p_elem.findall(".//TABLE")
    for node in tbl.iter()
}
for text_elem in p_elem.findall(".//TEXT"):
    if id(text_elem) in inside_table:
        continue
    ...
```

### A-3. Docling 표의 다단 헤더가 데이터 행으로 밀림

`rag/tables/docling.py`

병합셀을 좌상단 칸에만 채우고 span 을 전파하지 않아, 2단 헤더가 이렇게
나왔습니다.

```
| 구분     | 2023년 |       |   ← GFM 이 이 줄만 헤더로 인식
|----------|--------|-------|
|          | 예산   | 결산  |   ← 헤더 2단이 데이터 행이 됨
| 연구개발 | 1,240  | 1,198 |
```

`column_header` 플래그로 헤더 행 수를 세고, 헤더는 span 전체에 값을
전파한 뒤 열별로 병합하도록 고쳤습니다.

```
| 구분     | 2023년 예산 | 2023년 결산 |
|----------|-------------|-------------|
| 연구개발 | 1,240       | 1,198       |
```

HTML 경로(`converters/html/tables.py`)에는 이미 다단 헤더 병합이 있었는데
Docling 경로에만 없던 비대칭이었습니다. **표가 `wrong` 판정을 받는 주요
원인**이었으므로 LLM 재추출 호출 수도 함께 줄어듭니다.

---

## B. 판정 결과가 무시되던 것

### B-1. 표 품질 게이트 미적용

`rag/tables/fill.py:184`

```python
if ctype == "table" and fill_tables:
    _fill_table_with_llm(page, table, cfg, page_b64)
```

`assess` 가 매긴 `quality` 를 보지 않고 **모든 표를 재추출**했습니다.
`sufficient` 로 판정된 표까지 LLM 을 호출하므로 비용이 그대로 낭비됩니다.

`TableInfo.needs_fill`(`content_type == table` 이고
`quality in {wrong, insufficient}`)로 걸러내고, 전체 재추출은
`fill_all=True` 로 명시할 때만 하도록 분리했습니다.

### B-2. 이미지 그룹으로 흡수된 표가 메타에 남음

`_convert_group_to_image()` 가 본문에서 표 블록을 이미지 placeholder 로
바꾸면서 **어떤 표가 사라졌는지 알려주지 않았습니다.** 호출부는
`remaining_tables` 에 그대로 담아, 본문엔 없는 표가 `tables[]` 에 남는
orphan 이 생겼습니다.

제거된 id 집합을 함께 반환하도록 바꿨습니다. `ImageInfo` 도 호출부에서
`page.images` 에 추가되지 않아 유실되던 것을 함께 고쳤습니다.

---

## C. 실행이 실패하던 것

### C-1. 수식·코드 VLM 이 항상 켜짐

`converters/pdf/docling_backend.py:110-112`

```python
pipeline_options.do_formula_enrichment = True
pipeline_options.do_code_enrichment = True
```

표·본문 추출에는 쓰이지 않는데 별도 VLM 을 내려받고 `torch.compile` 까지
태웁니다. **Windows 비 UTF-8 로케일에서 파싱이 시작조차 못 하던 크래시의
직접 원인**이었습니다.

기본 꺼짐으로 바꾸고 `DOCLING_CODE_FORMULA_ENRICHMENT` 로 설정 가능하게
했습니다. 모델 로딩 시간도 줄어듭니다.

### C-2. `_log` 미정의 (NameError)

`converters/pdf/docling_backend.py` 에 `import logging` 도 `_log` 정의도
없는 상태에서 `_log.warning(...)` 을 호출하는 곳이 있었습니다. 전부 오류
처리 경로라 평소엔 드러나지 않고, **정작 문제가 생겼을 때 원인 메시지 대신
`NameError` 가 났습니다.**

### C-3. `find_spec` 이 예외를 던질 수 있음

`converters/deps.py:29`

```python
DOCLING_AVAILABLE = importlib.util.find_spec("docling") is not None
```

`sys.modules` 에 `__spec__` 이 없는 모듈이 있으면 `ValueError` 가 나면서
**모듈 import 자체가 실패**합니다. 예외를 잡고, 캐시가 낡았을 때를 위해
`invalidate_caches()` 후 재시도하도록 했습니다.

### C-4. Windows cp949 로케일 크래시

PyTorch inductor 의 템플릿 로더가 시스템 기본 인코딩으로 파일을 읽어,
cp949 환경에서 UTF-8 파일을 읽다 죽었습니다. `winfix.py` 로 호출 시점에
판정해 우회하고, 영구 해결(`PYTHONUTF8=1`)을 안내합니다.

---

## D. LLM 호출 관련

### D-1. 재시도·오류 표면화 없음

원본 `infrastructure/llm/client.py` 에는 재시도가 없었습니다. 429(사용량
한도)나 5xx(일시 오류)에 그대로 실패했고, 오류 본문도 버려져 원인을 알 수
없었습니다.

`Retry-After` 를 존중하는 재시도(최대 3회)와 오류 본문 파싱을 넣었습니다.

### D-2. 인증 헤더 누락

`llm_api_config()` 가 `api_key` 를 담지 않아, 키가 필요한 엔드포인트
(OpenAI 등)에서 유효한 키로도 401 이 났습니다.

### D-3. OCR 언어 코드 오류

```python
opts.lang = ["korean", "en"]   # rapidocr
```

`RapidOcrOptions.lang` 기본값이 `["chinese"]` 입니다 — 축약형이 아니라
전체 단어 규약이므로 `"en"` 이 아니라 `"english"` 여야 합니다.
엔진별 규약이 다르므로 `DOCLING_OCR_LANG` 로 설정 가능하게 했습니다.

| 엔진 | 규약 |
|------|------|
| rapidocr | `korean`, `english`, `chinese` |
| easyocr | `ko`, `en` |
| tesseract | `kor`, `eng` |

> 이 수정이 실제 인식률을 바꾸는지는 확인하지 못했습니다.
> `"en"` 이 조용히 무시됐을 뿐 한국어 인식엔 영향이 없었을 수도 있습니다.

---

## E. 진단이 불가능하던 것

원본에는 "왜 이렇게 나왔는지" 알 수 있는 수단이 없었습니다.
버그는 아니지만 문제 추적을 막던 구조적 결함이라 함께 정리합니다.

| 추가한 것 | 해결한 문제 |
|-----------|-------------|
| `PageTrace` / `TraceStep` | 페이지가 어떤 경로로 처리됐는지 (텍스트 레이어 / OCR / 재추출) |
| `layout.md` | 레이아웃 모델 라벨 vs 파이프라인 처리 결과 대조 — 오인식인지 변환 문제인지 구분 |
| `failed_pages` | Docling 이 로그로만 남기고 결과에서 조용히 빠뜨리던 페이지 |
| `timings` | 어느 단계가 느린지 (GPU 로 줄어드는 구간 표시) |
| `--check` | 환경·의존성·LLM 연결 상태 |

---

## F. 성능

| 항목 | 원본 | 수정 후 |
|------|------|---------|
| LLM 호출 | 순차 | `DOCLING_LLM_CONCURRENCY` 로 병렬 (실측 12초 → 2초) |
| 페이지 이미지 인코딩 | 표마다 반복 | 페이지당 1회 (스레드 안전 캐시) |
| 연산 장치 | 미설정 (CPU 고정) | `DOCLING_DEVICE` 로 지정 |
| 연결 불가 시 | 표마다 재시도 (15회) | 첫 실패 후 중단 (1회) |

---

## 검증 한계

- LLM 은 스텁으로 검증했습니다. 실제 사내 엔드포인트 호출은 확인하지
  못했습니다.
- Docling 실물·GPU 는 이 환경에 없어, 모의 객체로 옵션 구성 로직만
  확인했습니다.
- OCR 언어 코드 수정(D-3)의 실제 인식률 변화는 미확인입니다.

---

## G. 0.1.47 — 자체 검토에서 나온 것

앞의 A~F 는 원본 backend 대비 수정이고, 아래는 0.1.46 코드 자체를 다시
검토하며 찾은 것입니다.

### G-1. LLM 도달 불가 표시가 서버에서 영구 고착 (P0)

`infrastructure/llm/client.py`

`_UNREACHABLE` 에 유효 시간이 없고, 해제 경로가 `checks.reload_environment()`
하나뿐이었습니다. 이 함수는 노트북에서만 부르므로 FastAPI 워커에는 해제
수단이 없습니다. **LLM 이 한 번만 잠깐 끊겨도 그 프로세스가 사는 내내**
표 판정·재추출이 통째로 비활성화됐습니다.

CLI 는 실행이 짧아 "이번 실행에서만 건너뛴다" 가 맞았지만, 서버는 며칠씩
삽니다. 같은 코드가 배포 형태에 따라 다른 의미가 된 경우입니다.

`(url, model) → (사유, 표시 시각)` 으로 바꾸고 `UNREACHABLE_TTL`(기본 60초)
이 지나면 표시가 풀리게 했습니다. 로그는 표시가 새로 생길 때만 남습니다.

### G-2. 전역 정의 블록 중복 (P1)

같은 파일 86~92 행에 `_LOCAL_ANNOUNCED` 와 `CONNECT_TIMEOUT` 이 한 번 더
정의돼 있었습니다. 값이 같아 지금은 무해하지만, 위쪽 `CONNECT_TIMEOUT` 을
고치면 아래쪽이 조용히 되돌립니다. 제거했습니다.

### G-3. 임시 디렉터리 누수 (P0)

`pipeline.py`

`out_dir` 없이 실행하면 `mkdtemp()` 로 만든 폴더를 아무도 지우지 않았습니다.
`DocStructBatch` 로 N 건을 돌리면 N 개가 남습니다.

`weakref.finalize` 로 **PageDocument 수명에 묶었습니다.** 문서가 살아 있는
동안은 preview·save() 가 그 경로를 읽으므로 지우면 안 되고, 사라진 뒤에는
아무도 안 쓰므로 남기면 안 됩니다.

곁들여, `to_json()` 은 이미지를 옮기지 않아 JSON 안의 경로가 임시 폴더를
가리켰습니다. 지금까지는 그 폴더가 안 지워져서 우연히 살아 있었을 뿐입니다.
저장 시점에 JSON 옆으로 건져내도록 했습니다(`_rescue_scratch`).

### G-4. out_dir 없이 실행하면 표 재추출이 조용히 무력화 (P1)

`pipeline.py`

그림은 임시 폴더에라도 저장하면서 페이지 렌더는 `out_path is not None` 일
때만 했습니다. 재추출은 페이지 이미지를 근거로 삼으므로, `out_dir` 없이
부르면 `fill_tables=True` 여도 "재추출 불가" 만 남고 끝났습니다.
렌더도 같은 임시 폴더를 쓰게 맞췄습니다.

### G-5. HWP 리포트에 PDF 전용 단계명 (P2)

`models.py`

`STAGE_EXTRACT = "추출 (백엔드+레이아웃+TableFormer+OCR)"` 가 형식과 무관하게
쓰여, HWP/HWPX 의 `pipeline.md` 에도 TableFormer·OCR 이 찍혔습니다. 둘 다
안 도는데도요. `stage_extract(fmt)` 로 갈랐고, `GPU_ACCELERATED` 는 PDF
경로만 포함하도록 했습니다. `STAGE_ASSESS/FILL` 의 "(원격)" 도 로컬 VLM 일
때 거짓이라 뺐습니다.

### G-6. DocumentConverter 이중 생성 (P2)

`converters/pdf/docling_backend.py`

`@lru_cache` 는 조회만 원자적이고 **생성은 아닙니다.** 서버에서 첫 요청
두 개가 겹치면 Docling 모델이 두 번 로드됩니다(수 GB). 생성만 락으로
직렬화했습니다.

### G-7. `Any` 미임포트 (P1)

`core/config.py:843` 의 `dict[str, Any]` 에 임포트가 없었습니다.
`from __future__ import annotations` 덕에 문자열로 남아 안 터졌지만,
`typing.get_type_hints()` 나 pydantic 이 닿으면 즉시 `NameError` 입니다.
`tests/test_regressions.py::test_config_annotations_resolvable` 가 막습니다.

### G-8. `.env.example` 드리프트 (P1)

`config.py` 가 읽는 키 9개가 pkg 쪽 예시에 없었습니다 — 로컬 VLM 기능
전체(`DOCSTRUCT_VLM_*`)와 `DOCLING_TABLE_LLM_MODE`, `DOCLING_TABLE_FORMAT`,
엔드포인트별 키 등. 반대로 local 쪽에는 fallback 3키가 없었습니다.
서로 빠진 게 달랐습니다.

pkg 를 단일 소스로 합치고, `tools/sync_trees.py` 가 세 트리에 배포합니다.
`test_env_example_covers_all_keys` 가 재발을 막습니다.

### G-9. 잡다한 것

- `__init__.py` 의 최상위 `assert` → `RuntimeError`. `python -O` 에서
  통째로 사라져 검사가 없어지던 문제.
- `pipeline.py` / `checks.py` 의 중복 import 제거 (F811).
- `zip()` 에 `strict=` 명시, 플레이스홀더 없는 f-string 2건 정리.

### G-10. OCR 점검이 "설치됨" 을 잘못 보고 (P1)

`checks.py`, `converters/pdf/docling_backend.py`

`_installed()` 가 `importlib.util.find_spec()` 만 썼습니다. 이건 **파일이
있는지만** 보고 실제로 import 되는지는 확인하지 않습니다. 그래서 컨테이너에서
`rapidocr` 은 설치돼 있는데 `libGL.so.1` 이 없어 import 가 깨지는 상황을
"정상" 으로 보고했습니다.

그 상태에서 docling 은 `pip install rapidocr onnxruntime` 이라고 안내합니다.
docling 은 import 실패 원인을 구분하지 않기 때문인데, 이미 설치돼 있으므로
그 안내를 따라도 아무것도 해결되지 않습니다.

실제 import 를 시도하고, 공유 라이브러리 부재는 설치할 시스템 패키지까지
짚도록 했습니다.

    rapidocr 은 설치돼 있으나 시스템 라이브러리 libGL.so.1 가 없음
      — apt-get install -y libgl1

같은 진단을 컨버터 생성 시점에도 경고로 남깁니다. docling 의 잘못된 안내에
가려지지 않게 하기 위함입니다.

---

## H. 0.1.48 — 그림으로 잘못 분류된 표 되찾기

### 문제

레이아웃 모델이 표를 `PICTURE` 로 분류하면 TableFormer 가 돌지 않습니다.
그러면 그 영역의 내용이 **통째로 텍스트화되지 않고** 그림 파일 + VLM 캡션만
남습니다. 개정세법 대비표처럼 굵은 테두리와 대괄호가 많은 문서에서 자주
일어납니다.

    PictureItem picture l=69.3 t=627.0 r=491.0 b=467.1
    PictureItem picture l=69.4 t=386.5 r=490.9 b=171.8

두 영역 모두 본문 폭을 꽉 채우는 대비표인데 그림으로 잡혔습니다.

### 왜 어렵나

그림이 표인지 알려면 봐야 하는데, 모든 그림을 VLM 에 보내면 호출 수가 그림
개수만큼 늘어납니다. 로고·구분선까지 전부 물어보는 셈이라 시간·비용이 큽니다.

### 해결 — 3단계

**0단계. 무료 후보 선별** (`converters/pdf/text_probe.py`)

디지털 PDF 는 텍스트 레이어가 있으므로 영역 안 글자 수를 공짜로 셀 수
있습니다. 이미 쓰고 있는 pypdfium2 로 bbox 를 잘라 읽습니다.

    표 모양 영역 : 308자 · 7줄 · 면적비 13.5%
    사진 영역    :   0자 · 0줄 · 면적비  9.2%

면적비로는 안 갈리는데 글자 수로는 완전히 갈립니다. 사진·로고가 여기서
전부 떨어집니다.

좌표계 주의: docling 의 bbox 는 좌상단 원점, pypdfium2 는 좌하단 원점입니다.
변환 없이 넘기면 페이지 반대쪽을 읽어 항상 0자가 나옵니다.

**1단계. 승격 판정 — 추가 호출 없음** (`tables/assess.py`)

표 평가는 이미 페이지당 한 번 LLM 을 부르면서 **페이지 렌더 이미지와 본문을
둘 다 보내고 있습니다.** 후보 목록만 프롬프트에 얹으면 판정을 더 받을 수
있습니다. 호출 수가 늘지 않습니다.

**2단계. 재추출**

승격된 표는 `quality=insufficient` 로 표시되어 기존 fill 경로가 그대로
이어받습니다. fill 은 페이지 전체 이미지를 근거로 쓰므로 크롭도 불필요합니다.

### 그림도 함께 남깁니다

표 텍스트는 검색용, 원본 그림은 출처 확인용으로 둘 다 씁니다. 같은 영역이
`tables` 와 `images` 양쪽에 등록되므로 서로를 가리키는 참조를 뒀습니다.

- `TableInfo.source_image_id` → 원본 그림
- `ImageInfo.promoted_table_id` → 승격된 표

본문에서는 그림 placeholder 바로 뒤에 표 블록이 들어가 읽기 순서를 지킵니다.

### TableFormer 로 되돌리지 않은 이유

크롭 이미지와 text cell 을 `TableStructureModel` 에 직접 먹여야 하는데 이건
docling 공개 API 가 아니라 버전마다 깨집니다.

더 중요한 건 품질입니다. 이런 대비표는 셀 안에 `□ / ○ / – / •` 계층 불릿과
여러 행을 묶는 대괄호, `(좌 동)` 대응 관계가 있습니다. TableFormer 는 셀
격자를 내보내는 모델이라 이 구조가 전부 뭉개집니다. VLM 에 markdown 으로
받아 계층을 불릿으로 살리는 편이 낫습니다.

### 조정

    MIN_CHARS = 80    # text_probe.py — 후보 최소 글자 수
    MIN_LINES = 3     # 후보 최소 줄 수

스캔 PDF 는 텍스트 레이어가 없어 이 신호를 쓸 수 없습니다. 그 경우 후보가
잡히지 않으므로 승격도 일어나지 않습니다.

### H-2. 재추출에 PDF 원문을 함께 넘김

승격된 표는 markdown 파싱 결과가 비어 있어 페이지 이미지만으로 재추출해야
합니다. 그런데 이미지만 주면 `➊➋➌`, `․`(가운뎃점), `§2①` 같은 글자를 VLM 이
잘못 읽습니다.

0단계에서 이미 영역 원문을 읽었으므로 그것을 함께 넘깁니다. 실제 문서에서
추출한 원문:

    종전 개정(안)
    □ 조특법상 중소기업 제외대상 □ 제외업종 등 추가
    ○ 소비성서비스업 ○ (좌 동)
    <추 가>
    ○ 부동산임대업
    ...

다만 원문만으로는 부족합니다. 좌우 열이 한 줄에 붙어 나오고, 항목 수가 다른
표에서는 순서가 뒤엉킵니다(왼쪽 4개 뒤에 오른쪽 `○ (좌 동)` 이 붙는 식).

그래서 역할을 나눠 지시합니다 — **글자는 원문에서, 행·열 구조는 이미지에서.**
`ImageInfo.region_text` 에 보관하고 `TableInfo.source_image_id` 로 찾아갑니다.
일반 표에는 붙지 않습니다.

---

## I. 0.1.49 — HWP 폴백 오탐 (P0)

### 증상

    pyhwp HTML 이 불충분해 olefile 텍스트 폴백으로 처리 — 표·그림 구조가 손실됩니다

정부 성과계획서(626KB, 표 67개)에서 표가 **전부** 사라졌습니다.

### 원인

`assess_pyhwp_html()` 이 stderr 문자열만 보고 결과를 확인하지 않았습니다.

    if "unmatched field end" in stderr.lower():
        return True          # ← HTML 을 보지도 않고 즉시 폴백

실측한 판정 조건 대조:

    ① 'unmatched field end' in stderr : True   ← 유일하게 걸린 것
    ② 본문 500자 미만                 : False  (8,476자)
    ③ 빈 셀 과다                      : False  (780셀 중 629개 채워짐)

`hwp5html` 은 returncode 0 으로 **성공**했고 표 67개를 정상 생성했습니다.
pyhwp 는 `PatternTypeEnum value: 6`, `UnderlineStyle value: 15` 같은 경고를
흘리면서도 변환을 정상으로 끝내는 일이 흔한데, `unmatched field end` 도
같은 부류였습니다. 원래 의도는 "필드 문서에서 **내용이 비는** 경우"를 잡는
것이었지만 결과 검사가 빠져 있었습니다.

### 수정

경고를 즉시 폴백 사유가 아니라 **의심 신호**로 낮췄습니다.

    필드 경고 + 내용 있는 셀 10개 이상 → 폴백 안 함 (표 손실이 더 큼)
    필드 경고 + 본문 1,000자 미만      → 폴백
    경고 없음 + 본문 500자 미만        → 폴백 (기존과 동일)

측정 결과:

    수정 전 (olefile 폴백)  49,931자 · 표 행   0
    수정 후 (pyhwp HTML)    29,771자 · 표 행 178

폴백 쪽 글자 수가 더 많은 것은 개선이 아닙니다. `謔̃`, `岤̃` 같은 깨진
제어문자가 섞여 글자 수를 부풀린 것입니다 — olefile 직접 파싱이 레코드
구분자를 텍스트로 잘못 읽습니다.

파이프라인 전체로는 표 48개 · 본문 30,953자가 복원됩니다.

### 사유를 남깁니다

이번 진단이 오래 걸린 이유는 **어느 조건에 걸렸는지 로그에 없었기**
때문입니다. `pyhwp_html_verdict()` 가 (여부, 사유) 를 함께 돌려주도록 하고,
로그·트레이스·`PageTrace.notes` 에 실었습니다.

    pyhwp HTML 사용: 필드 경고가 있으나 표가 살아 있음
                     (표 67개 · 내용 있는 셀 629개) — HTML 그대로 사용

`assess_pyhwp_html()` 은 여부만 돌려주는 얇은 wrapper 로 남겨 기존 호출부를
깨지 않습니다.

### 남은 것

복원된 표에는 표지의 "2027" 을 세로쓰기로 한 글자씩 배치한 것 같은 레이아웃
용도 표가 섞입니다. 구조적으로는 표가 맞아 자동 제거가 어렵고, 표 판정
LLM 의 `content_type: text` 경로가 걸러낼 영역입니다.

---

## J. 0.1.50 — OLE 텍스트 깨짐 · 전면 OCR 접근성

### J-1. HWP 폴백에서 탭이 글자를 깨뜨림 (P1)

`converters/hwp/olefile.py`

HWP 5.0 본문의 문자 제어는 길이가 세 가지입니다.

    단독 1글자 : 0, 10(줄바꿈), 13(문단끝), 24~31
    인라인 8글자: 4~9, 19, 20            ← 9 = 탭
    확장 8글자  : 1~3, 11, 12, 14~18, 21~23

뒤 둘은 `제어코드 + 부속 12바이트 + 제어코드 반복` 구조라 12바이트를
건너뛰어야 하는데, 목록에서 **9(탭), 19, 20 이 빠져 있었습니다.**

    0009 5B88 0000 0303 0000 0000 0000 0009 000D
     ↑    └──── 건너뛰지 못한 12바이트 ────┘
    탭

탭을 `code < 32` 로 흘려보내면서 오프셋을 옮기지 않아, 부속 데이터가 다음
글자로 읽혔습니다 — `守`(U+5B88) + `̃`(결합 틸데) 같은 것이 그렇게 나옵니다.

정부 성과계획서(49,688자)로 측정:

    수정 전: 이상문자 50개  (̃ 21, 乄 6, 守 2 …)
    수정 후: 이상문자 13개  (11개는 軍·後·前·中 등 실제 한자)

탭은 버리지 않고 `\t` 로 남깁니다. 목차의 "항목 ⟶ 쪽번호" 처럼 열을 가르는
경우가 많아, 지우면 글자가 통째로 붙습니다.

참고로 확장 제어(1~3)의 부속 데이터에는 4글자 컨트롤 ID 가 들어 있어
이렇게 보입니다 — 이쪽은 원래 목록에 있어 정상 처리됐습니다.

    0002 6364 7365 ...  →  "捤獥"  (= "secd", 구역 정의)

### J-2. 전면 OCR 을 Colab 에서 켤 수 없었음 (P2)

`colab.py`

PDF 폰트의 ToUnicode 매핑이 깨지면 텍스트 레이어에서 엉뚱한 글자가 나옵니다.
이건 OCR 오류가 아니라 PDF 안에 그렇게 들어 있는 것이라, 텍스트 레이어를
무시하고 전면 OCR 하는 수밖에 없습니다.

`DOCLING_FORCE_FULL_PAGE_OCR` 스위치는 이미 있었지만 `colab.configure()` 에
노출돼 있지 않아 Colab 사용자가 환경변수를 직접 만지고 `rebuild_settings()`
와 `invalidate_caches()` 까지 불러야 했습니다.

    colab.configure(..., force_full_page_ocr=True)

설정 요약에도 표시합니다.

    OCR 백엔드: rapidocr · 전면 OCR (텍스트 레이어 무시)

---

## K. 0.1.51 — HWP 폴백에 미리보기 스트림 활용

### 배경

`hwp5html` 이 본문을 못 읽으면 olefile 텍스트 폴백으로 내려가는데, 이 경로는
문단 텍스트(태그 67)만 읽어 **표 구조가 통째로 사라집니다.**

    파일 54,784bytes → hwp5html 결과: 표 0개 · 본문 39자
    같은 파일 → olefile 폴백: 1,030자 (표 없음, 세로쓰기는 한 글자씩 분해)

레코드에는 표 컨트롤(태그 71·72)이 멀쩡히 있는데 폴백이 안 읽는 것입니다.

### 착안

한글이 저장할 때 만들어 둔 미리보기 스트림에 구조 정보가 이미 있습니다.

    PrvText  : <재정성과책임관><><백승보 청장><>   ← 셀 경계가 그대로
    PrvImage : 첫 페이지 미리보기 (PNG 또는 JPEG)

공짜로 얻을 수 있고 추가 의존성도 없습니다.

### 한계 — 커버리지 판정이 핵심

`PrvText` 는 **1,023자에서 잘립니다.** 두 파일 모두 정확히 같은 길이인 것으로
상한을 확인했습니다.

    복사본: 본문  1,030자 · 커버리지 83.7%  → 사용
    원본  : 본문 49,688자 · 커버리지  1.4%  → 거부

긴 문서에 쓰면 앞 2% 만 표가 되고 나머지가 없는 것이 되어 오히려 손해입니다.
`PrvImage` 도 첫 페이지뿐이라, HWP 가 문서 전체를 1개 PageContent 로 다루는
구조에서 긴 문서에 붙이면 LLM 이 뒷부분 표를 첫 페이지 그림으로 판단하게
됩니다. 그래서 둘 다 `coverage() >= 0.8` 일 때만 적용합니다.

### 결과

    수정 전: 표 0개 · 본문 1,115자 · 페이지 이미지 없음
    수정 후: 표 3개 · 본문 1,642자 · 페이지 이미지 있음

원본(49,688자)은 커버리지 1.4% 로 정확히 거부되어 기존 동작을 유지합니다.

`PrvImage` 는 표 재추출(fill)의 시각 근거가 됩니다. HWP 는 페이지 렌더가
불가능해 지금까지 fill 이 근거 이미지 없이 돌았습니다.

### 남은 것

`PrvText` 는 복잡한 표를 한 줄로 뭉개는 경우가 있어(원본 표 전체가 한 행이
되는 식) 행 구조가 완전하지 않습니다. 셀 경계는 살아나므로 재추출 LLM 이
다듬을 근거로는 충분합니다.

근본 해결은 OLE 레코드의 표 컨트롤(HWPTAG_TABLE 71, HWPTAG_LIST_HEADER 72)을
트리로 읽는 것인데, HWP 5.0 규격을 상당 부분 구현해야 하는 별도 작업입니다.

---

## L. 0.1.52 — 균등배분 복원 · 한컴 PUA 매핑

kordoc(MIT, chrisryugj)의 접근을 참조해 이식했습니다. PUA 매핑표의 원 출처는
rhwp(MIT, edwardkim)의 한컴 PDF 시각 검증 테이블입니다.

### L-1. 균등배분 복원

제목을 칸에 맞추려고 글자를 벌려 쓴 것을 되붙입니다.

    대 한 민 국 정 부      → 대한민국정부
    프 로 그 램 논 리 모 형  → 프로그램논리모형

HWP 세로쓰기 셀, PDF 자간 벌림 제목에서 같은 형태로 나타납니다.

**기준을 kordoc 보다 엄격하게 잡았습니다.** kordoc 은 "토큰의 70% 이상이
1글자" 면 줄 전체를 붙이는데, 그러면 이렇게 됩니다.

    홍 보 담 당 관 회의 자료  → 홍보담당관회의자료   (7토큰 중 5개 단일 = 71%)
    제 1 장 총칙            → 제1장총칙           (4토큰 중 3개 단일 = 75%)

**모든** 토큰이 1글자일 때만 줄 전체를 붙이고, 나머지는 "1글자가 3개 이상
연속" 구간만 정규식으로 처리합니다.

    홍 보 담 당 관 회의 자료  → 홍보담당관 회의 자료
    제 1 장 총칙            → 제1장 총칙

2자 단어는 어느 규칙에도 안 걸립니다 — `중동 사태 대응` 을 균등배분으로 보면
`중동사태대응` 이 되어 정상 문장이 망가집니다. kordoc 이 실제로 겪은 실패라
같은 방어를 넣었습니다.

### L-2. 한컴 PUA 매핑

글머리표·기호가 사용자 정의 영역(U+F020~U+F0FF, U+F0000대)에 저장되어
있어 그대로 내보내면 빈 네모가 됩니다.

    \uf06f → □      \uf0a2 → ○      \uf0fc → ✔

**매핑에 없는 BMP PUA 는 그대로 둡니다.** 옛한글(한양 PUA)일 수 있어
제거하면 글자가 사라집니다.

### 적용 범위

HWP(olefile·PrvText), HWPX, PDF 본문 조각 — 네 경로 모두에 걸었습니다.
표 셀처럼 짧은 조각에는 `normalize_korean_text(..., collapse=False)` 로
균등배분 복원만 끌 수 있습니다.

### 측정 (2027년도 성과계획서, 626KB)

    적용 전: ... 2027년도 성과계획서 (조달청) □ 예산안 성과계획서 대 한 민 국 정 부
    적용 후: ... 2027년도 성과계획서 (조달청) □ 예산안 성과계획서 대한민국정부

---

## M. 0.1.53 — HWP 기본 경로를 파서 트리로 교체 (P0)

### 우리가 잘못된 층을 쓰고 있었습니다

pyhwp 안에는 층이 둘입니다.

    hwp5.binmodel / xmlmodel   ← 파서
            ↓ XSLT 변환
    hwp5html                   ← HTML 생성기   ← 지금까지 여기를 씀

626KB 정부 성과계획서로 실측한 차이:

    경로                    표      순수 글자     속도
    hwp5html               48      7,170      26.3초
    hwp5.xmlmodel         119     47,644       1.0초
    olefile 평문            0      36,070       0.1초

`hwp5html` 이 **표의 60%, 본문의 85%를 버리면서 26배 느립니다.**
XSLT 단계에서 깎이는 것이고, 문서에 따라 통째로 실패하기도 합니다
(54KB 복사본에서 표 0개·본문 39자).

중복 생성이 아닌 것도 확인했습니다 — 23개 섹션, TableControl 119개
(중첩 8개, 최대 깊이 3), Text 조각 6,690개.

pyhwp 가 오래되어 생긴 문제가 아닙니다. PyPI 최신이 0.1b15 이고 그것이
설치본인데, **같은 버전의 파서는 두 파일 모두 완벽하게 읽습니다.**

### 새 경로 (`converters/hwp/hwp5tree.py`)

경로 우선순위가 넷이 됐습니다.

    hwpml-xml    → hwp5-tree → pyhwp-html → olefile-text
    (XML 문서)     (기본)      (폴백)       (최후)

트리 결과가 200자 미만이거나 예외가 나면 자동으로 다음 경로로 넘어갑니다.
`hwp5html` 을 지우지 않고 폴백으로 남긴 것은, 문서 한 건으로 판단하기
이르고 실패 시 대안이 있는 편이 안전하기 때문입니다.

파이프라인 실측:

    원본  : 경로=hwp5-tree · 표 111개 · 본문 63,091자 · 1.1초
    복사본: 경로=hwp5-tree · 표   3개 · 본문  1,593자 · 0.1초

### 중첩 표 — 표식 방식

처음에는 중첩 표를 부모 셀 안에 그대로 넣었는데 읽을 수 없었습니다.

    | \| 분야 \| 요인 \| 시사점 \| \| --- \| --- \| --- \| \| 정치 …

GFM 은 셀 안에 표를 담지 못해 한 줄로 눕고 `|` 가 이스케이프됩니다.
사람도 LLM 도 읽을 수 없습니다.

부모 셀에는 `[중첩표 1]` 표식만 남기고 본체는 부모 표 바로 뒤에 둡니다.
관계는 유지되고 표는 표대로 읽힙니다.

    | [중첩표 1] 4 |  |  |

    [중첩표 1]

    | 분야 | 요인 | 시사점 |
    | --- | --- | --- |
    | 정치 (Political) | ▪ ’25년3월 개통된 … | ▪ 차세대나라장터에 … |

### 병합 셀

왼쪽 위 칸에만 값을 넣고 나머지는 비웁니다. GFM 이 병합을 표현하지 못하는데
값을 복제하면 같은 내용이 검색에 여러 번 걸립니다.

### 곁들여

pyhwp 의 `hwp5.bintype` 이 모델 타입을 컴파일할 때마다 INFO 로그를 남겨
문서 하나에 수십 줄이 쏟아졌습니다. 경고 이상만 남기도록 눌렀습니다.

### 남은 것

표지처럼 표를 레이아웃 용도로 쓴 경우("2027" 을 세로로 한 글자씩 배치)가
그대로 표로 잡힙니다. 구조적으로는 표가 맞아 자동 제거가 어렵고,
표 판정 LLM 의 `content_type: text` 경로가 걸러낼 영역입니다.

---

## N. 0.1.54 — 세로쓰기 복원 · 그림→표 승격 재검증

### N-1. 세로쓰기 복원

표 셀에 제목을 세로로 배치하면 낱글자 줄이 이어져 나옵니다.

    프                 프로그램논리모형
    로       →
    그
    램 …

0.1.52 의 균등배분 복원은 **공백**으로 벌어진 것만 다뤄서 줄바꿈으로
나뉜 세로쓰기를 못 잡았습니다.

3줄 이상 연속일 때만, **한글·숫자만** 대상으로 붙입니다. 기호(`○`)를
포함하면 표의 한 글자 값이 우연히 이어질 때 오작동합니다.

실측 (`__성과목표관리_추진체계.pdf` 1페이지):

    세로쓰기 구간 2개 → 0개
    '…백호성\n구매사업국장\n프로그램논리모형\n투입 (input) ⇒ 활동 …'

markdown 표 행(`| 가 |`)은 낱글자 줄이 아니므로 영향받지 않습니다.

### N-2. 그림→표 승격 재검증

표가 통째로 `PictureItem` 으로 잡히는 PDF 로 0.1.48 의 승격 경로를
다시 확인했습니다.

    1페이지: 468×567pt · 글자 533자 · 줄 63 · 표 후보 ○
    2페이지: 485×210pt · 글자 261자 · 줄 19 · 표 후보 ○

두 페이지 모두 0단계(무료 텍스트 밀도 필터)를 통과합니다. 승격 후
재추출 프롬프트에는 PDF 텍스트 레이어 원문이 그대로 실립니다.

    '26예산(A) '27예산안(B) 증감(B-A)
    정부조달 국제협력체제
    구축(1233)
    금액 9,729 7,952 △1,777
    (비중) 8.0 5.2 △2.7
    …

숫자가 원문 그대로 전달되므로 OCR 오독 없이 재구성할 수 있습니다.
행·열 구조는 첨부 페이지 이미지로 판단하도록 프롬프트에 명시돼 있습니다.

### 주의 — 표가 아닌 것도 승격될 수 있습니다

1페이지는 조직도와 논리모형 흐름도(투입⇒활동⇒산출⇒결과)입니다.
구조상 표로 만들 수는 있지만 의미상 도표입니다. 승격 판정을 LLM 이
하므로 그런 것까지 표로 만들 수 있는데, **그림도 함께 남기므로**
원본 확인은 가능합니다(0.1.48 의 양쪽 유지 설계).

---

## O. 0.1.55 — 그림 영역을 셋으로 가른다 (P0)

### 두 갈래로는 조직도가 갈 곳이 없었습니다

0.1.48 의 승격 경로는 결과가 둘뿐이었습니다.

    그림으로 둔다  → 조직도 533자가 통째로 사라짐
    표로 승격한다  → 흐름도가 격자로 뭉개짐

`__성과목표관리_추진체계.pdf` 1페이지가 정확히 그 경우입니다. 조직도와
논리모형 흐름도(투입⇒활동⇒산출⇒결과)라 표가 아니지만, 글자는 많습니다.

게다가 판정을 LLM 에 맡기고 있어 도표까지 표로 만들 수 있었습니다.

### 좌표만으로 갈립니다 (`converters/pdf/region_kind.py`)

같은 PDF 두 페이지를 실측했습니다.

    1페이지 조직도+흐름도 : 최빈 2열(52%) · 열 시작 x 편차 최대 112.0pt
    2페이지 예산 대비표    : 최빈 5열(73%) · 열 시작 x 편차 최대   9.4pt

**열 개수만으로는 안 갈립니다.** 둘 다 다중 셀 줄이 있습니다. 결정적인 것은
**열 시작 x 의 일관성** 입니다 — 표는 같은 열이 매 줄 같은 자리에서
시작하고, 도표는 상자가 제각기 놓여 있어 흔들립니다.

판정 결과:

    1페이지 → TEXT  | 열 시작 위치가 줄마다 어긋남 (편차 112pt) — 도표로 보입니다
    2페이지 → TABLE | 5열이 8줄 반복 · 열 정렬 편차 9pt

LLM 호출이 없습니다.

### 세 번째 갈래 — 도표 텍스트를 본문에 넣습니다

TEXT 로 판정되면 그림 placeholder **바로 뒤에** 원문을 넣습니다. 그림도
그대로 남으므로 원본 확인이 됩니다.

    □ 성과목표관리 추진체계
    <!-- image 1 -->
    재정성과책임관 백승보 청장
    프로그램논리모형
    투입 ⇒ 활동 ⇒ 산출 ⇒ 결과 및 효과

세로쓰기 복원(0.1.54)도 함께 걸려 `프/로/그/램` 이 붙습니다.

### 판정 기준

    최빈 열수를 가진 줄 ≥ 3
    최빈 열수 ≥ 2
    최빈 열수가 다중셀 줄의 50% 이상
    열 시작 x 표준편차 ≤ 12pt
    → 넷 다 만족해야 TABLE

### 문턱을 표와 텍스트로 분리했습니다

    표 후보 문턱   : 80자 · 3줄  (text_probe.MIN_CHARS)
    텍스트 문턱    : 30자 · 2줄  (region_kind.MIN_TEXT_CHARS)

파이프라인 도표처럼 상자마다 짧은 레이블만 있으면 67자 정도가 나옵니다.
표 문턱을 그대로 쓰면 그 글자가 통째로 사라집니다 — 표로 만들 만큼은
아니어도 본문으로 뽑을 값어치는 있습니다.

좌표를 읽지 못하면 IMAGE 로 봅니다. 판단 근거가 없으면 그대로 두는 편이
안전합니다.

### 판정 사례 (실측)

    실제 조직도+흐름도      → TEXT   열 시작 편차 112pt — 도표
    실제 예산 대비표        → TABLE  5열이 8줄 반복 · 편차 9pt
    벡터 파이프라인 도표     → TEXT   열 시작 편차 34pt — 도표
    래스터 파이프라인 도표    → IMAGE  글자 0자 — 캡처 이미지
    짧은 레이블 도표(19자)   → IMAGE  글자 19자 — 캡션 수준

조직도와 파이프라인 도표를 **구분하지 않습니다.** 판정하는 것은
"무엇을 그린 것인가" 가 아니라 "격자로 표현할 수 있는가" 이고, 둘 다
아니오라서 같은 처리(그림 유지 + 텍스트 추출)를 받는 것이 맞습니다.

실제로 갈리는 지점은 **벡터인가 래스터인가** 입니다. 같은 도표라도 캡처
이미지로 붙였으면 글자 좌표가 0개라 IMAGE 가 됩니다. 이 경우 내용 복원은
그림 설명 VLM 의 몫입니다.

### LLM 의 역할이 바뀌었습니다

    이전: 그림 후보를 전부 LLM 에 물어 표/이미지 판정
    이후: 좌표로 표/도표/사진을 먼저 가르고, TABLE 만 LLM 에 올림

LLM 호출이 줄고, 도표가 표로 오판될 여지도 사라집니다.

---

## P. 0.1.57 — IMAGE 는 버리는 것이 아니라 VLM 으로 읽는다

### 문제

0.1.55 의 3분류에서 IMAGE 는 사실상 **포기** 였습니다. 캡처 이미지로 붙인
표·조직도는 글자 좌표가 0개라 IMAGE 로 가는데, 그러면 내용이 통째로
사라집니다. 실측에서 그 경우가 실제로 나왔습니다.

    래스터 파이프라인 도표 → IMAGE | 글자 0자 — 사진·로고로 둡니다

### 해결 (`media/vlm_read.py`)

저장해 둔 **그림 파일 자체**를 근거로 VLM 에 내용을 옮겨 달라고 합니다.
페이지 전체가 아니라 해당 그림만 보내므로 판독률이 높습니다.

    표      → GFM 표로
    조직도·흐름도 → 계층 목록 + `→` 연결
    그 밖   → 보이는 글자를 읽기 순서대로

그림 placeholder 바로 뒤에 넣고 **그림은 그대로 남깁니다.**

### 그림 설명과 다릅니다

docling 의 그림 설명은 한 문장 캡션입니다("조직도를 나타낸 그림").
여기서는 내용을 옮깁니다. 목적이 검색·인용이라 캡션으로는 쓸모가 없습니다.

### 대상 선별

호출 수를 늘리지 않도록 넷을 모두 만족할 때만 보냅니다.

    · region_kind 가 image (표·도표는 각자의 경로가 맡는다)
    · 저장된 그림 파일이 있다 (근거가 없으면 물어볼 수 없다)
    · 페이지 면적의 3% 이상 (로고·아이콘 제외)
    · 아직 읽지 않았다

읽을 내용이 없으면 모델이 `내용 없음` 을 돌려주도록 지시했고, 그 경우
본문을 건드리지 않습니다. 응답이 20자 미만이어도 실패로 봅니다.

### 옵션

    build_document(..., read_pictures=False)   # 끄기

기본은 켜짐입니다. LLM 미설정이면 조용히 건너뜁니다. 단계 소요 시간은
`STAGE_PICTURE_READ` 로 `doc.timings` 에 남습니다.

### 이제 세 갈래 모두 내용을 남깁니다

    TABLE → 표로 복원 (승격 → fill)
    TEXT  → 텍스트를 본문에 삽입
    IMAGE → VLM 으로 읽어 본문에 삽입   ← 이번에 추가

---

## Q. 0.1.58 — 그림 처리 경로를 하나로 (기존 기능은 유지)

### 중복이었습니다

그림 설명(docling 내장)이 이미 있었는데 0.1.57 에서 내용 추출을 따로
만들어, 큰 그림은 **두 번 호출**됐습니다.

    docling 그림 설명 : 면적 1% 이상 전부 · "조직도를 나타낸 그림입니다"
    vlm_read         : 면적 3% 이상 · 실제 표·목록

목적이 다릅니다. 기존 것은 **캡션**이고 새 것은 **내용**입니다. 검색·인용에는
캡션이 쓸모가 적지만, 로고·아이콘 설명에는 캡션이 맞습니다. 그래서 지우지
않고 고를 수 있게 했습니다.

### DOCSTRUCT_PICTURE_MODE

    read     (기본) 내용을 옮긴다 — vlm_read 만
    describe docling 캡션만
    both     둘 다 (비용 두 배)
    off      그림에 LLM 을 쓰지 않는다

read 를 기본으로 둔 이유는 셋입니다.

  · 호출이 한 번으로 줄어든다
  · 대상 선별을 우리가 통제한다 (면적·판정 결과·중복 방지)
  · docling 내부 호출은 재시도가 5회 고정이라 서버가 느리면 그림 하나에
    수 분씩 멈춘다. vlm_read 는 우리 클라이언트를 타서 도달 불가 표시·
    대비 엔드포인트가 걸린다

모드 확인은 docling import **앞**에 둬서 미설치 환경에서도 동작합니다.

---

## R. 0.1.59 — HWP 렌더러 보강 (제목·목록·강조)

### HWPX 가 더 나은 이유는 형식이 아니라 렌더러였습니다

`hwpx.tools.markdown_export` 는 18KB 짜리로 제목·목록·강조·각주·하이퍼링크·
도형 텍스트를 다 다룹니다. 우리 `hwp5tree` 는 문단과 표만 봤습니다.

그래서 HWP→HWPX 변환이 아니라 **렌더러를 채우는 쪽**을 골랐습니다.
변환 경로는 `HWP → 우리 파서 → HWPX → markdown` 이라 우리 파서가 천장이고,
왕복해도 얻는 게 없습니다.

### 무엇을 읽는가 (`converters/hwp/styling.py`)

DocInfo 에 재료가 다 있었습니다. 626KB 문서 실측:

    Style 46개 · CharShape 1,168개 · ParaShape 639개 · Numbering 23개

`Paragraph.style_id` 와 `Text.charshape_id` 로 연결합니다.

### 제목 — 스타일만으로는 안 됩니다

한글 기본 스타일에 `개요 1~7` 이 있지만, **실제 공문서는 거의 안 씁니다.**
이 문서가 쓰는 스타일은 이랬습니다.

    바탕글(310회) · 바탕글 사본3(277회) · xl68(34회) · 1. 내용(25회)

글자 크기로도 안 갈립니다 — 본문이 15pt 이고 그보다 큰 것이 거의 없습니다.
크기로 제목을 추정하면 표 안 강조와 표지 큰 글씨가 전부 제목이 됩니다.

그래서 **번호 표기**를 함께 봅니다. `제N장/절/편`, `Ⅰ.` 형태이고 60자
이내일 때만 제목으로 올립니다. 파생 스타일(`개요 6 사본1`)도 접두로 잡습니다.

### 목록 — 공문서 글머리 계층

    □ → ○ → - → *   순으로 수준이 내려간다

기호가 곧 수준이라 들여쓰기 정보 없이 계층이 복원됩니다.

### 표 셀 안에서는 적용하지 않습니다

`#` 나 `- ` 를 GFM 표 셀에 넣으면 표가 깨집니다. 셀 안에서는 강조만 씁니다.

### 결과 (2027년도 성과계획서, 626KB)

    보강 전: 제목  0 · 목록   0 · 강조   0 · 표 119
    보강 후: 제목  3 · 목록 394 · 강조 306 · 표 119

제목이 3개뿐인 것은 문서 자체에 `제N장` 이 3번밖에 없기 때문입니다. 이
문서는 `□` 계층으로 구조를 나타내고, 그것은 목록 394개로 반영됐습니다.

### 안 한 것

각주·하이퍼링크·도형 텍스트는 아직 안 읽습니다. pyhwp 트리에 정보는
있으므로 필요해지면 같은 방식으로 추가할 수 있습니다.

---

## S. 0.1.60 — HWP 페이지 분리 · 중첩표 통번호

### S-1. 중첩표 번호가 겹쳤습니다

부모 표마다 1부터 다시 세어, 같은 `[중첩표 1]` 이 문서에 **12번** 나왔습니다.
어느 표를 가리키는지 알 수 없습니다.

    수정 전: Counter({'1': 12, '2': 2, '3': 2})
    수정 후: 1~8 고유 (각 번호가 셀 표식·본체로 2회씩)

문서 전체 통번호로 바꿨습니다.

### S-2. 페이지 분리

HWP 는 렌더링 시점에 쪽이 정해져 **파일에 페이지 경계가 없습니다.** 그래서
지금까지 문서 전체가 한 덩어리(67,531자)로 나왔습니다.

다만 나눌 근거가 아주 없지는 않습니다.

    Paragraph.split 의 new_page / new_section 비트 : 명시적 쪽나눔(Ctrl+Enter)
    BodyText/SectionN                              : 구역 구분 (이 문서는 23개)

둘을 합쳐 36쪽으로 나뉩니다.

    1쪽:     90자 · 표 1개 [1]
    3쪽:    313자 · 표 4개 [3, 4, 5, 6]
    4쪽:  1,332자 · 표 4개 [7, 8, 9, 10]

**인쇄된 쪽 번호와는 다릅니다.** 자동 줄바꿈으로 넘어간 쪽은 파일에 기록이
없어 복원할 수 없습니다. 그래서 `page_no_kind` 를 `"document"` 로 둬서 물리
쪽이 아님을 드러냅니다(PDF 는 `"physical"`).

### 표 번호는 통번호를 유지합니다

쪽으로 가르면서 표 번호를 다시 매기면 본문의 `<table N>` 과 어긋납니다.
번호는 그대로 두고 해당 쪽으로 나눠 담습니다.

    표 번호 유일: True · 오름차순: True

표 안의 쪽나눔은 무시합니다 — 표가 두 쪽으로 쪼개집니다.

### 쪽 표식

폼피드(`\x0c`)를 씁니다. 본문에 나올 일이 없고, 공백류라 눈에 띄지 않습니다.
다만 `strip()` 에 걸리므로 정규화 단계에서 따로 보존해야 했습니다.

---

## T. 0.1.61 — 배치에서 빈 결과가 조용히 성공 처리되던 문제

### 증상

    batch.summary()  → 성공 2건, 실패 0건
    JSON             → 본문 없음

예외가 없으면 배치는 성공으로 셉니다. 그런데 본문이 비면 실패 목록에도
안 뜨고 JSON 만 텅 빈 채로 남아, 나중에야 알아차리게 됩니다.

### T-1. `read_pictures` 가 옵션 목록에 없었습니다

0.1.57 에서 `build_document` 에만 추가하고 `_RUN_KEYS` 에 넣지 않아,
`DocStructBatch(..., read_pictures=False)` 가 이렇게 죽었습니다.

    DocStructError: 알 수 없는 설정 키: 'read_pictures'

### T-2. 읽을 수 없는 HWP 를 미리 짚습니다 (`converters/hwp/diagnose.py`)

`FileHeader` 플래그와 스트림 구성을 보고 원인을 알려 줍니다.

    암호 문서        → "암호가 걸린 문서입니다 — 한글에서 암호를 풀고 저장하세요"
    배포용(DRM)      → "본문이 암호화되어 있습니다 — 일반 문서로 저장하거나
                        .hwpx 로 내보내세요"
    OLE 가 아님       → "HWP 3.0 이하이거나 확장자만 .hwp 인 다른 형식"
    BodyText 없음    → "본문 스트림이 없습니다 (ViewText 만 있음)"

배포용 문서는 본문이 `BodyText` 가 아니라 `ViewText` 에 암호화되어 들어갑니다.
pyhwp 는 예외 없이 빈 결과를 돌려주므로 지금까지 "성공" 으로 처리됐습니다.

이제 `ValueError` 로 올려 배치의 `failures` 에 잡힙니다.

**판단이 애매하면 막지 않습니다.** 진단 자체가 실패하면 `readable=True` 로
두어, 잘 되던 문서가 갑자기 실패하는 일을 피합니다.

### T-3. 본문이 비면 경고를 남깁니다

진단을 통과했는데도 결과가 비는 경우가 있습니다.

    WARNING 문서.hwp: 본문이 0자뿐입니다 — 추출에 실패했을 수 있습니다.
            배포용(DRM) 문서이거나 파서가 내용을 읽지 못한 경우입니다

표가 하나라도 있으면 경고하지 않습니다.


## 0.1.62 (2026-08-07)

코드 전반 적합성 검수 결과 반영 + 전 함수 docstring 규약(역할·입력·출력·동작) 완비.

### 수정
1. **json_parse.parse_json_list_or_object_map — 약한 LLM 비정형 응답 크래시**
   `{"table_1": "sufficient"}` 처럼 값이 dict 가 아닌 객체 맵에서 `**v` 가
   TypeError 를 내고, 목록에 문자열이 섞이면 호출부 `.get()` 이
   AttributeError 를 냈다. 두 경로 모두 dict 만 남기도록 가드했다.
   해당 페이지의 표 판정이 통째로 생략되던 문제.
2. **extractors/hwpx + converters/hwpx — python-hwpx 7.0 대비**
   `export_rich_markdown()` 은 6.0 에서 deprecated, 7.0 에서 제거된다.
   `doc.text.markdown(rich=True)` 신 API 를 먼저 시도하고 구버전에
   폴백하는 `rich_markdown()` 을 converters/hwpx 에 두고 양쪽이 공유한다.
3. **hwp5tree.to_markdown — Hwp5File 핸들 미해제**
   try/finally 로 close 한다. Windows 에서 처리 직후 원본 삭제·이동이
   막히고 서버 프로세스에서 핸들이 누적되던 문제.
4. **hwp5tree/styling — 첫 런 charshape 로 문단 전체 강조**
   문단 안 런들의 charshape 가 균일할 때만 강조를 적용한다 (mixed 추적).
   첫 단어만 굵은 문단이 통째로 굵어지던 문제.
5. **hwp5tree._render_table — 선언 열 수보다 큰 col 의 셀 유실**
   TableBody.cols 와 셀 좌표 실측치 중 큰 값을 쓴다. 과소 선언 문서에서
   범위 밖 셀이 조용히 버려지던 문제.

### 정리
- ruff F401 미사용 import 8건 제거
- 전 함수(중첩 포함) docstring 에 입력/출력, 필요시 동작·비고 명시 — 커버리지 100%
- 회귀 테스트 7건 추가 (총 112건)
- 배포 zip 에서 __pycache__/.ruff_cache 제외

## 0.1.63 (2026-08-07) — 0.1.62 회귀 긴급 수정

### 원인
0.1.62 검수에서 ruff F401("미사용 import") 8건을 `--fix` 로 일괄 정리했는데,
그중 `colab.py` 의 다음 줄은 죽은 코드가 아니라 **노트북용 재노출**이었다.

    from docstruct.checks import check_llm_reachable   # noqa: E402

바로 위 주석이 "기존 코드·노트북과의 호환을 위해 재노출한다"고 적어 두었는데도
정적 분석 결과만 보고 지웠다. 노트북의
`colab.check_llm_reachable(timeout=15)` 이 AttributeError 로 죽었다.

교훈: 모듈 안에서 안 쓰인다는 것과 지워도 된다는 것은 다르다. 재노출은
호출부가 그 모듈 **밖**에 있어서 정적 분석에 잡히지 않는다.

### 조치
- 지운 import 8건 전부 복원. 재노출 성격인 것에는 `# noqa: F401` 을 붙여
  앞으로 `ruff --fix` 가 다시 지우지 못하게 했다.
- 회귀 테스트 7건 추가 (총 119건):
  · 재노출 심볼 5개 실존 확인 (parametrize)
  · `colab.check_llm_reachable is checks.check_llm_reachable` 동일성
  · **노트북 소스에서 `colab.*`/`checks.*` 참조를 긁어 전부 해석되는지 검사**
    — 노트북과 코드의 계약을 테스트가 직접 고정한다.
- 검증: import 를 일부러 지우면 위 3개 테스트가 실패함을 확인했다.

## 0.1.64 (2026-08-07) — 문서(.md) 호출 전수조사

0.1.63 의 `colab.check_llm_reachable` 사고와 같은 유형이 문서에도 있는지
md 전체를 뒤졌다. 코드블록에서 API 호출·import·CLI 플래그·trace 라벨을
뽑아 실제로 해석되는지 대조했다.

### 검사 범위와 결과

| 항목 | 건수 | 결과 |
|---|---|---|
| 최상위 심볼·preview 함수·모델 속성 | 67 | 통과 |
| `configure()`/`set()` 설정 키·키워드 인자 | 15 | 통과 |
| md 의 모듈·속성 경로 | 29 | **1건 실패** |
| CLI 플래그 | 10 | 통과 |
| 실행 로그 예시의 trace 라벨 | 9 | 통과 |
| local(4-패키지) 레이아웃 문서 단언 | 6 | 통과 |

`MERGE_TO_BACKEND.md` 가 "조치 완료" 라고 단언한
`core.config.docling_picture_api_status` 도 실존을 확인했다.

### 수정 — WINDOWS.md `from docstruct import winfix`

문서가 이 한 줄을 안내하는데 그 경로가 없었다 (실제 위치는
`docstruct.core.winfix`). 노트북은 `from docstruct.core import winfix` 로
올바르게 쓰고 있어 드러나지 않았다.

문서를 고치는 대신 **`winfix` 를 패키지 최상위에 재노출**했다.
`preview`/`report` 가 이미 쓰는 방식이고, 패키지 배포본과 backend
4-패키지 배치 양쪽에서 문서의 그 한 줄이 그대로 통한다.

### 오탐으로 판정한 것 (수정 없음)

- `ds.set(gpu=True)` (API.md) — "오타는 즉시 잡힙니다" 를 보여주는
  **의도된 에러 예시**. 바로 아래 줄이 DocStructError 를 적고 있다.
- `from docstruct.models.page import ...` (RESTRUCTURE.md) — 채택하지
  않은 구조 개편안의 가정 코드.
- `docstruct.git` / `docstruct.exe` — 저장소 URL과 실행파일 이름.

### 재발 방지 (총 123건)

- `test_doc_module_paths_resolve` — md 의 점 표기와 **`from X import Y`
  표기** 양쪽을 검사한다. 처음엔 점 표기만 봐서 정작 winfix 회귀를
  놓쳤고, from-import 를 넣어 잡히는 것을 확인했다.
- `test_doc_cli_flags_exist` — 문서의 CLI 플래그를 `--help` 와 대조
- `test_doc_trace_labels_exist` — 로그 예시 라벨을 `trace.add()` 와 대조
- `test_winfix_importable_from_package_root`

## 0.1.66 (2026-08-07) — pyhwp 필드 경고 홍수

### 조사 — `unmatched field end`

DocStructBatch 로 성과계획서를 돌릴 때 이 메시지가 쏟아진다는 제보.
출처는 pyhwp 의 `hwp5/xmlmodel.py:440`, `logger.warning('unmatched field end')`
였다. 필드(누름틀·쪽번호·상호참조)의 시작/끝 짝이 안 맞을 때 나온다.

**치명적이지 않다.** `mfse_field_end` 는 짝 없는 종료 이벤트를 버리고 그대로
진행한다 — 예외가 아니므로 `batch.failures` 에도 잡히지 않는다. 본문 손실도
없다. 실제로 빈 리스트를 돌려주는 것을 확인했다.

문제는 양이다. `_quiet_pyhwp()` 가 `hwp5.bintype`·`binmodel`·`filestructure`
만 잠재우고 **`hwp5.xmlmodel` 을 빠뜨려서**, 문단마다 한 줄씩 찍혔다.
수백 건 배치에서는 수천 줄이 되어 진짜 오류를 덮는다.

### 수정

통째로 가리면 신호가 사라지므로 **세어서 문서당 한 번만 요약**한다.

    INFO  필드 짝 경고 47건 (누름틀·쪽번호 등) — 본문에는 영향 없습니다

`_FieldWarningCounter` 는 해당 문구만 걸러내고 다른 경고는 통과시킨다.

### 그 과정에서 잡은 결함

계수기를 떼는 일을 호출부 `finally` 에 맡겼더니, **`Hwp5File()` 이 먼저
실패하는 경로에서 필터가 로거에 남았다** (try 진입 전이라 finally 가 안 돈다).
배치로 손상 파일을 계속 만나면 필터가 무한히 쌓인다. 추가한 누수 테스트가
이걸 잡아서, 붙이고 떼는 짝을 `_field_warnings()` 컨텍스트 매니저로 묶었다.

### DocStructBatch 검증 (별도 결함 없음)

사용자 경로 구조(한글·공백·괄호·후행 슬래시)를 그대로 재현해 확인했다.
`pattern="*.hwp"` 필터링, 손상 파일 격리, `summary()`, `failures`,
`to_json("결과/")`, `to_json(..., combined=True)` 모두 정상.

### 테스트 5건 추가 (총 131건)

## 0.1.67 (2026-08-07) — `undefined UnderlineStyle value: 15`

0.1.66 에서 `unmatched field end` 만 다뤘는데, 같은 성격의 다른 경고가
`hwp5.dataio` 에서 더 많이 나온다는 지적. 조사 결과 **경고 자체는 사실이나
우리 추출에는 영향이 없다.**

### 근거 — CharShape 비트 배치

    Flags = Flags(UINT32,
                  0,       'italic'            ← 우리가 읽는 것
                  1,       'bold'              ← 우리가 읽는 것
                  2, 3,    Underline
                  4, 7,    UnderlineStyle      ← 경고가 나는 곳
                  ...)

`underline_style` 은 **4~7비트, 즉 4비트라 0~15** 를 담을 수 있는데 pyhwp 의
Enum 표는 **0~10 만** 정의한다. 값 15 는 문서가 깨진 게 아니라 **pyhwp 표가
비어 있는 것**이다. 실패 시 동작도 안전하다 — `dataio.py:319` 가
`int.__new__(cls, value)` 로 원시 정수를 그대로 돌려주고 예외를 내지 않는다.

italic(0비트)·bold(1비트)은 별개 비트라 값이 보존되는 것을 시험으로 확인했다
(`test_undefined_enum_does_not_affect_bold_italic`).

### 수정 — 삼키지 않고 요약

0.1.66 의 계수기를 `hwp5.dataio` 까지 넓히고 종류별 집계로 바꿨다.
2,383줄이 한 줄이 된다.

    INFO  pyhwp 반복 경고 요약 — undefined UnderlineStyle value: 15 1,168건 ·
          unmatched field end 47건 (본문·표에는 영향 없음.
          원문을 보려면 DOCSTRUCT_PYHWP_VERBOSE=true)

설계 원칙 세 가지를 지켰다.

1. **모르는 경고는 통과시킨다.** `_NOISY_PATTERNS` 에 걸리는 것만 삼킨다.
   모르는 경고까지 가리면 진짜 문제가 조용히 묻힌다.
2. **삼킨 것도 버리지 않는다.** 종류별로 세어 문서당 한 줄로 요약한다.
3. **원문을 볼 수 있다.** `DOCSTRUCT_PYHWP_VERBOSE=true` 면 계수기를 아예
   달지 않아 pyhwp 경고가 그대로 나온다.

`defined name/values:` 덤프(한 번에 열 줄 넘는 Enum 표)는 앞 줄에 딸린
것이라 세지 않고 버린다.

### 테스트 3건 추가 (총 134건)

## 0.1.68 (2026-08-07) — 판정하지 않은 표를 "판정 완료" 로 보고

72쪽 성과계획서 결과 JSON 검토 중 발견. 표 212개가 **전부**
`quality: sufficient` 인데 `timings.표 평가 LLM = 0.0`, `pipeline.llm_url = None`
이었다. LLM 이 한 번도 호출되지 않았는데 trace 에는
`[ok] docstruct.tables.assess · LLM 표 판정` 으로 남아 있었다.

결과 JSON 만 보면 212개 표가 품질 검증을 통과한 것처럼 보인다. 이 파일이
RAG 색인이나 검수 근거로 넘어가면 잘못된 신뢰가 그대로 전달된다.

### 원인

`assess_page_tables` 는 LLM 이 없으면 모든 표를 `_mark_default()` 로
`table/sufficient` 표시만 하고 끝난다. `pipeline` 은 그 사실을 모른 채
성공으로 기록했다.

**두 경로가 있는데 처음엔 하나만 잡았다.**

  1. LLM 미설정 — `llm_available()` False
  2. **엔드포인트는 설정됐으나 연결 불가** — `llm_available()` 은 True 다.
     사내망 밖(Colab 등)에서 돌리면 늘 이 경로다. 이쪽이 더 위험하다 —
     설정이 되어 있어 정상으로 보인다.

`llm_available()` 만 보면 2번을 놓친다. 그래서 **실제 결과 데이터**를 보고
판단하도록 바꿨다.

### 수정

- `_mark_default(table, unassessed=True)` 가 `reason` 에 사실을 남긴다.
  `미판정 — LLM 없이 기본값으로 표시 (품질을 확인한 것이 아님)`
- LLM 호출이 예외로 끝난 경우도 `unassessed=True` 로 전달한다.
- `pipeline` 은 표의 `reason` 을 보고 trace 를 남긴다.

      [skip] 표 판정 생략 — LLM 응답 없음(미설정 또는 연결 불가) —
             212개를 기본값으로 표시했을 뿐, 품질을 확인한 것이 아닙니다

두 경로 모두 재현해 확인했다. 테스트 3건 추가 (총 137건).

### 함께 확인한 것 (수정 없음 — 판단 필요)

같은 문서에서 관찰된 표 품질 문제. 원인이 서로 달라 별도로 다뤄야 한다.

- **머리행이 빈 표 24/212 (11%)** — HWP 표의 첫 행이 병합·여백 행이면
  `_render_table` 이 그것을 GFM 헤더로 올려 빈 헤더가 된다.
- **셀 텍스트 조각화 49/212 (23%)** — `**년** **도**`, `**프** **로**`.
  좁은 칸에서 작성자가 Enter 로 줄을 나눈 것을 셀 블록마다 강조를 걸어
  이어 붙여서 생긴다. 붙일 때 공백을 넣을지가 한국어에서는 모호하다
  (`성과관리대상 사업` vs `성과관리대상사업`).
- **JSON 문자열 누출 2건** — `{"fields": {},"simplefields": {}}`.
  212개 중 2개로 국지적이며, 문서 자체의 필드 값일 가능성이 있다.

## 0.1.69 (2026-08-07) — hwp5html 실패가 문서를 통째로 죽이던 문제

`2027년도 성과계획서_[06]중앙선거관리위원회.hwp 처리 실패: hwp5html 실패:`
뒤에 pyhwp 경고만 잔뜩 붙어 있어, 그 경고가 원인처럼 보였다. 아니었다.

### 경로 순서

`HwpConverter.to_markdown()` 은 이 순서로 시도한다.

    1. is_hwpml()             → XML 이면 hwpml-xml 로 종료
    2. _get_tree_markdown()   → hwp5-tree (기본). 실패하면 None
    3. _uses_ole_fallback()   → **여기서 hwp5html 을 실행한다**
    4. olefile-text 또는 pyhwp-html

### 원인 ① — 폴백이 작동하지 않음

3번은 "olefile 로 내려갈지" 를 판정하는 함수인데, 그 판정을 위해 hwp5html 을
돌린다. 그런데 `HwpTimeout` 만 잡고 **`RuntimeError`(종료코드 ≠ 0)는 잡지
않아** 예외가 그대로 위로 튀어 문서가 통째로 실패했다.

hwp5html 실패는 olefile 로 내려갈 **가장 강한 근거**다. 본문은 건질 수
있었는데 아무것도 못 건졌다. 이제 잡아서 폴백한다.

### 원인 ② — 오류 메시지가 원인을 가림

    raise RuntimeError(f"hwp5html 실패:\n{result.stderr.strip()}")

stderr 가 `undefined … value` / `unmatched field end` 로 가득 차 있어 진짜
오류 줄이 묻혔다. 사용자가 경고를 실패 사유로 읽게 되는 자리다.

`real_error_lines()` 로 상시 경고를 걷어내고 실제 오류만 싣는다. 걷어낸 뒤
아무것도 남지 않으면 **원인을 특정하지 못했다는 사실 자체를 밝힌다** —
경고를 원인인 양 보여주는 것보다 낫다.

### 앞선 "영향 없다" 판단에 대하여

0.1.67 에서 `undefined UnderlineStyle value: 15` 가 추출에 영향이 없다고 한
것은 **성공한 실행** 기준이며 그 판단 자체는 유효하다 (`dataio.py:319` 가
원시 정수를 그대로 돌려주고 예외를 내지 않는다). 다만 이번 사례처럼 **실패
메시지에 섞여 원인을 가리는** 경우가 있다는 점을 놓쳤다. 경고가 무해한 것과,
경고가 무해하다고 알려주는 것으로 충분한 것은 다르다.

### 테스트 3건 추가 (총 140건)

## 0.1.70 (2026-08-07) — 첫 실패가 묻혀 두 번째 실패만 보이던 문제

"폴백까지 내려갔다는 건 애초에 뭔가 실패했다는 뜻 아니냐" 는 지적. 맞다.
`_uses_ole_fallback()` 에 도달했다는 것은 **기본 경로(hwp5-tree)가 이미
실패했다**는 뜻이고, 그게 첫 실패다.

그런데 그 실패는 INFO 였다.

    _log.info("pyhwp 파서 트리 경로 실패 — 기존 경로로 넘어갑니다: %s", exc)

기본 로깅(WARNING)에서 보이지 않으니, 사람 눈에는 두 번째 실패
(`hwp5html 실패:` + pyhwp 경고 더미)만 남았다. 그래서 경고를 원인으로
읽게 됐다. 0.1.69 에서 메시지를 정리했지만 **첫 실패를 드러내지 않는 한
반쪽짜리**였다.

### 수정

- 기본 경로 실패를 **WARNING** 으로 올리고 예외 종류까지 남긴다.
- 사유를 `tree_failure` 프로퍼티에 보존한다 (본문이 짧아 포기한 경우 포함).
- 폴백까지 실패하면 최종 사유에 **첫 실패를 함께 싣는다**.

      hwp5html 실행 실패 — … / 앞서 hwp5-tree 도 실패: KeyError: 'HWPTAG_LIST_HEADER'

두 경로는 같은 pyhwp 파서(`hwp5.xmlmodel`)를 공유한다. hwp5html 은 그 위에
XSLT 를 얹은 것뿐이라, 둘 다 실패했다면 원인이 같을 가능성이 높다. 먼저
죽은 쪽이 진짜 원인에 가깝다.

### 테스트 3건 추가 (총 143건)

## 0.1.71 (2026-08-07) — trace 객체 공유로 JSON 의 85%가 중복

72쪽 성과계획서 결과 JSON(4.3MB) 검토 중 발견. 모든 쪽의 trace.steps 가
**203개로 동일**했고 1쪽과 72쪽의 내용이 완전히 같았다.

### 원인

`extractors/hwp._split_by_page_break()` 가 쪽마다 PageContent 를 만들면서
`trace=trace` 로 **같은 PageTrace 객체를 넘겼다.** 이후 단계가 쪽마다 남기는
기록이 한 리스트에 쌓이고, 그 리스트가 쪽 수만큼 직렬화됐다.

    docstruct.tables.tags    5,184 = 72쪽 × 72번
    docstruct.tables.assess  4,824
    docstruct.tables.fill    4,464
    합계 14,616 steps · 2.5MB (파일의 85%)

쪽별 기록을 구분할 수 없다는 점이 더 큰 문제였다 — 어느 쪽에서 판정이
생략됐는지 알 수 없었다.

### 수정

`_clone_trace()` 로 쪽마다 독립 PageTrace 를 만든다. 분할 전 공통 기록
(어느 경로로 파싱했는지)은 복사해 남기고, 이후 단계는 쪽마다 따로 쌓인다.
`table_count` 도 쪽별 실제 표 개수로 바로잡았다.

    14,616 steps → 283 steps  (98% 감소)
    JSON 4.3MB → 575KB

### 함께 추가 — `slim=True` 출력

"모듈 정보가 너무 많아 파악이 힘들다" 는 요청. `to_dict`/`to_json_str`/
`to_json` 에 `slim` 을 넣었다. trace·layout·pipeline·timings 를 빼고
본문·표·그림만 남긴다.

    ds.to_json("결과.json", slim=True)
    batch.to_json("결과/", slim=True)

정보를 지우는 게 아니라 가린다 — 진단이 필요하면 slim 없이 뽑으면 된다.
표는 `{id, table_num, title, markdown}` 로, 쪽 처리 경로는 `extraction`
한 줄 요약으로 줄인다.

### 테스트 5건 추가 (총 148건)

## 0.1.72 (2026-08-07) — 표 렌더 정확성

원본 `2027년도 성과계획서_[03]국회.hwp` 를 pyhwp 로 직접 파싱해 셀 단위로
대조했다. **데이터 유실은 0%** — 비어 있지 않은 셀 4,433개가 전부 살아
있었다. 문제는 표현이었다.

### 발견 — 강조 기호가 셀 텍스트를 끊는다

좁은 칸에서 작성자가 Enter 로 줄을 나누면 블록마다 강조가 걸려
`**프로그램목표Ⅰ-1** **의정활동의 …**` 가 된다. 미관 문제가 아니다.
셀 중간에 낀 `**` 가 문자열 매칭을 깨뜨려, **대조 검증에서 멀쩡한 셀
75개가 유실로 오판됐다.** 검증 스크립트가 속았다면 RAG 색인도, 표 품질을
판정하는 LLM 도 같은 이유로 잘못 읽는다. insufficient 판정 31건 중 상당수가
여기서 왔을 가능성이 크다.

### 수정

- `_merge_emphasis()` — 인접한 강조 조각을 하나로 합친다.
  **글자와 공백은 건드리지 않는다.** `년 도` 를 `년도` 로 붙일지는 문서마다
  답이 달라(`성과관리대상 사업` 은 띄어야 맞다) 추측하면 안 된다.
  기호를 걷어낸 기준으로 결과 텍스트는 이전과 완전히 같다.
- 맨 앞의 **완전히 빈 행**은 헤더로 쓰지 않는다. 값이 없는 행이라 버려도
  잃는 정보가 없다. 표 전체가 비어 있으면 그대로 둔다(원본이 장식용 상자).

### 측정 (같은 문서, 표 212개)

    조각난 강조가 있는 표     49 → 0
    머리행이 빈 표            24 → 12   (남은 12개는 원본이 완전히 빈 상자)
    기호 때문에 매칭 실패한 셀  75 → 26   (남은 26개는 셀 일부만 굵은 정상 케이스)
    데이터 유실                0 → 0    (유지)

### 함께 추가 — `hwp_fill_html`

hwp5-tree 로 성공한 문서는 표 재추출 근거(`table_html_fragments`)가 빈
목록이라 **판정에서 insufficient 가 나와도 고칠 방법이 없었다.**
정확성이 속도보다 중요한 작업을 위해 옵션을 뒀다.

    ds = DocStruct(path, hwp_fill_html=True).run()

같은 문서 실측: 근거 0개 → 114개, 2.4초 → 126초. 기본값은 False 다.

### 테스트 7건 추가 (총 155건)

## 0.1.73 (2026-08-07) — 0.1.72 의 일반화 결함

"다른 형식 문서도 문제없이 동작해야 한다" 는 지적으로 재검토. 0.1.72 의
강조 병합이 **원문 별표를 파괴**하는 결함이 있었다.

### 결함

`_merge_emphasis()` 는 셀 텍스트 **전체를 정규식으로** 훑어
`**` 짝을 지웠다. 그래서 우리가 두른 굵게와 원문에 원래 있던 별표를
구분하지 못했다.

    `*****` 만 담긴 셀        → 빈 칸이 됨 (5,000자가 0자로)
    `주1** **주2`            → `주1 주2` (원문 기호 소실)

실제 성과계획서에도 별표가 23회 있었다 — `96.25*0.4(Ⅰ) + 74.9*0.6(Ⅱ)`
같은 측정산식과 `* 주:` 각주다. 이 문서에서는 `**` 가 인접하지 않아
드러나지 않았을 뿐, 다른 문서에서는 깨진다.

### 수정 — 문자열이 아니라 구조로 판단

`_join_cell_blocks(blocks)` 로 바꿨다. 셀 안 **문단 목록**을 받아
양끝이 `**` 인 블록(= styling.format_paragraph 가 두른 것)만 골라 합친다.
원문 텍스트 중간의 별표는 애초에 검사 대상이 아니다.

    ["**년**", "**도**"]        → "**년 도**"
    ["*****"]                  → "*****"            (보존)
    ["96.25*0.4(I)"]           → "96.25*0.4(I)"     (보존)
    ["**A**", "중간", "**B**"]  → "**A** 중간 **B**"  (합치지 않음)

### 일반화 검증

- 형식별 스모크: hwpx(python-hwpx) · hwp(hwpml-xml) · hwp(hwp5-tree)
  모두 정상, 조각화 0.
- 이번 수정이 닿는 범위는 hwp5-tree 경로뿐이다. HWPX·PDF·HWPML 은
  각자 다른 렌더러를 쓴다.
- 표 구조 경계: 빈 행 연속·중간 빈 행·병합 헤더·음수/역전/초과 좌표·
  헤더 없는 표 — 모두 확인.
- 성능: 조각 5,000개 1초 미만.

### 실문서 재측정 (동일)

    데이터 유실   0 / 4,433
    조각화 표     0 / 212
    빈 머리행 표  12 / 212   (원본이 완전히 빈 상자)
    계산식 보존   ✅

### 테스트 13건으로 확대 (총 152건)
