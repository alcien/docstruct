# ─────────────────────────────────────────────────────────────────────
#  docstruct 의존성
#  버전은 하한선만 지정합니다. 특정 조합을 검증한 것이 아니므로,
#  운영에 올릴 때는 동작 확인 후 `pip freeze > requirements.lock` 하세요.
# ─────────────────────────────────────────────────────────────────────

# --- 문서 파싱 공통 ---
beautifulsoup4>=4.12
requests>=2.31
python-dotenv>=1.0

# --- HWP (바이너리) ---
#  pyhwp 는 sdist 만 제공됩니다. 설치 시 `hwp5html` 실행파일이 함께 깔리며,
#  Windows 에서는 <venv>\Scripts\hwp5html.exe 로 들어갑니다.
#  이게 PATH 에 없으면 표 구조가 유실된 olefile 폴백으로 떨어집니다.
pyhwp>=0.1b15
olefile>=0.47
six>=1.16

# --- HWPX ---
python-hwpx>=1.0

# --- PDF (Docling) ---
docling>=2.26
pypdfium2>=4.30
pillow>=10.0
rapidocr-onnxruntime>=1.4      # DOCLING_OCR_BACKEND=rapidocr (기본값)

# tesseract 백엔드를 쓸 때만 필요한 시스템 패키지:
#   Linux : apt install tesseract-ocr tesseract-ocr-kor tesseract-ocr-eng libgl1
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki 설치 후 PATH 등록

# --- 노트북 (선택) ---
# jupyterlab>=4.0
# ipywidgets>=8.0

# --- 사내 LLM 어댑터 (선택) ---
#  미설치 시 requests 폴백으로 동작합니다.
# llm_request @ git+https://<사내-git>/ai-engineering/llm_request.git@<commit>
